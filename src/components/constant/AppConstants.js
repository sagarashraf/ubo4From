export const DAYLIST = Array.from({ length: 31 }, (v, k) => k + 1);
export const MONTHLIST = Array.from({ length: 12 }, (v, k) => k + 1);
export const YEARLIST = Array.from({ length: 50 }, (v, k) => k + 1);
